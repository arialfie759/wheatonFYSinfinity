#include <iostream>
#include <fstream>
#include <stacks>
using namespace std;

const int CLASS_MAX;
const int NUM_FYS_CLASSES;

struct wheatonFY{
  string firstName;
  string lastName
  int month;
  int day;
  int hour;
  int mins;
  int secs;
  bool fallAthlete;
  bool springAthlete;
  bool science;
  int classOne, classTwo, classThree, classFour, classFive, classSix, classSeven, classEight;
  //string identity;
  //string nationality;
};
  
int main(){

  ifstream inFP;
  ofstream outFP;
  
  cout << "Enter file name containing list of Wheaton first-years and their choices" << endl;
  inFP >> fileName;
  
  if (inFP){
    inFP.open(fileName);
    
  } 
  else{
    cout << "FILENAME IS INCORRECT, TRY AGAIN." << endl;
  }
}

